# DistilBERT 微調深度解析

> **目標**：不只會用，而是理解每一行代碼背後的「為什麼」
> 
> **時間投入**：30 分鐘讀完，之後每次訓練前複習一遍，3 次後會變成肌肉記憶
> 
> **核心問題**：
> 1. `max_seq_length` 該選 64、128 還是 256？
> 2. `compute_metrics` 怎麼寫才算真正懂情感分類？
> 3. 訓練完的那堆文件裡，誰才是真正的「模型」？

---

## 📊 實訓完成狀態

✅ **訓練完成**（2026-03-24）
- 模型：DistilBERT (distilbert-base-uncased, 67M 參數)
- 數據集：IMDB 情感分類（平衡數據，1500+1500）
- **測試準確率：90.4%**
- 訓練時間：~4 分鐘（RTX 3050）
- 訓練策略：分層採樣 + Early Stopping
- **max_seq_length 對照實驗：已完成（64 / 128 / 256）**

| 指標 | 數值 |
|------|------|
| 測試準確率 (Accuracy) | 90.40% |
| 加權 F1 分數 (F1 Weighted) | 90.39% |
| 負面評論 - 精確度 | 93% |
| 負面評論 - 召回率 | 88% |
| 正面評論 - 精確度 | 88% |
| 正面評論 - 召回率 | 93% |

📁 **生成的評估結果**（每個實驗目錄都有）：
- `results-maxlen-064/results/`
- `results-maxlen-128/results/`
- `results-maxlen-256/results/`

每個 `results/` 內都包含：
- `eval_results.json` - 機器可讀完整指標
- `evaluation_report.md` - 人類可讀評估報告
- `confusion_matrix.png` - 混淆矩陣圖
- `evaluation_metrics.png` - 指標對比圖

---

## 🧠 Part 1：核心概念速查

### 1.1 max_seq_length 的取捨（128 vs 512）

**問題**：為什麼不用 512（BERT 的最大長度）？

**答案**：這不是規則，而是**成本-效益平衡**。

```python
# GPU 內存計算公式
Memory_per_batch = batch_size × seq_length × hidden_size × precision_bits / 8

# 舉例（DistilBERT）
# hidden_size = 768（DistilBERT 的隱藏層維度）
# precision_bits = 32（float32）

# 若用 max_seq_length=512:
Memory = 8 × 512 × 768 × 32 / 8 = ~1.6GB（僅僅一個 forward pass！）
# 加上梯度、優化器狀態、activation 記憶，至少需要 8GB
# 在 8GB GPU 上，會直接 OOM

# 若用 max_seq_length=128:
Memory = 8 × 128 × 768 × 32 / 8 = ~400MB（舒服訓練！）
```

**IMDB 數據的現實**：
```python
# 實測 IMDB 評論的長度分布
from datasets import load_dataset
dataset = load_dataset("imdb")

token_lengths = [len(tokenizer.encode(text)) for text in dataset['train']['text']]
import numpy as np

print(f"平均長度: {np.mean(token_lengths):.1f}")   # 約 230 tokens
print(f"中位數: {np.median(token_lengths):.1f}")    # 約 180 tokens
print(f"95 百分位: {np.percentile(token_lengths, 95):.1f}")  # 約 320 tokens
print(f"總長度 > 512 的比例: {sum(l > 512 for l in token_lengths) / len(token_lengths) * 100:.1f}%")  # < 2%
```

**理論結論**：
- IMDB 95% 的評論 < 320 tokens
- 設 128 會截斷 ~3-5% 的長尾評論
- **取捨**：損失 1% 的邊界案例，換來 4 倍內存節省 → 能用 8GB GPU 訓練
- 實務判斷：對於短文本任務（評論、推特、新聞標題），128 是黃金點

**你的實測結論（已完成）**：

| max_seq_length | Accuracy | F1 | 訓練時間 |
|---|---:|---:|---:|
| 64  | 0.8157 | 0.8157 | 3.41 分鐘 |
| 128 | 0.8737 | 0.8736 | 4.03 分鐘 |
| 256 | 0.9030 | 0.9029 | 4.05 分鐘 |

在你的資料與硬體上，**256 幾乎不增加時間，但精度明顯更好**，因此目前採用 256 是合理的。

---

### 1.2 Truncation 的影響（你需要親自體驗）

**重點**：截斷真的會損失信息嗎？

```python
# 假設這個例子
review = "This movie is absolutely terrible, waste of time, BUT the last 5 minutes plot twist was mind-blowing..."

# 用 max_length=128
# → 前 128 tokens 會被保留："This movie is absolutely terrible, waste of time, BUT the last 5 minutes..."
# → "plot twist was mind-blowing" 被截掉了！
# → 模型只看到負面詞，沒看到正面轉折

# 問題：這會讓 F1 score 下降嗎？
# 答案：取決於你的數據
#   - IMDB 評論：很少有中間有重大轉折的（通常意見一貫）→ 影響小
#   - 複雜評論：長段落表達複雜想法 → 影響大

# 你該做的實驗：
# Exp 1: max_length=128 → 記錄 F1 score
# Exp 2: max_length=256 → 再看一次 F1 score
# Exp 3: 手工讀 10 個被截斷的評論，看看損失的信息重要嗎？
```

---

### 1.3 Compute Metrics 的三層遞進

**層級 1：最幼稚的版本（不推薦）**
```python
def compute_metrics(eval_pred):
    predictions, labels = eval_pred
    predictions = np.argmax(predictions, axis=1)
    
    accuracy = (predictions == labels).mean()
    return {'accuracy': accuracy}
```
❌ **問題**：
- 只看準確率，忽視了類別不平衡
- 如果訓練數據 60% 正面、40% 負面，模型可以籠統地全猜「正面」達到 60% 準確率
- 對於情感分類，完全沒看到「有多少負面被誤判為正面」

---

**層級 2：有點腦子（我給你的版本）**
```python
from sklearn.metrics import accuracy_score, f1_score

def compute_metrics(eval_pred):
    predictions, labels = eval_pred
    predictions = np.argmax(predictions, axis=1)
    
    return {
        'accuracy': accuracy_score(labels, predictions),
        'f1': f1_score(labels, predictions, average='weighted'),
    }
```
✅ **優勢**：
- F1 = 2 × (Precision × Recall) / (Precision + Recall)
  - Precision：說「正面」時，有多準？
  - Recall：所有真正的「正面」，我能抓到多少？
  - 平衡兩者，不被多數類主導
- `average='weighted'`：按類別比例加權
  - 如果數據 60% 正面、40% 負面
  - F1 = 0.6 × F1_positive + 0.4 × F1_negative

**為什麼 weighted 而不是 macro？**
```python
# 假設：2000 個正面、1000 個負面
# 模型結果：
#   正面識別率：0.90 (F1_pos = 0.90)
#   負面識別率：0.50 (F1_neg = 0.50)

# macro (不考慮數據比例):
#   F1 = (0.90 + 0.50) / 2 = 0.70

# weighted (按比例):
#   F1 = (0.90 × 2/3) + (0.50 × 1/3) = 0.60 + 0.167 = 0.767
#   → 更接近實際性能（因為我們有 2/3 的正面）

# 實務建議：用 weighted
#   - 反映真實業務場景（數據本身就不平衡）
#   - 不會被少數類的完美預測迷惑
```

---

**層級 3：專業人士版本（你該學的）**
```python
from sklearn.metrics import accuracy_score, f1_score, precision_score, recall_score, confusion_matrix

def compute_metrics(eval_pred):
    predictions, labels = eval_pred
    predictions = np.argmax(predictions, axis=1)
    
    return {
        'accuracy': accuracy_score(labels, predictions),
        'f1': f1_score(labels, predictions, average='weighted'),
        'precision': precision_score(labels, predictions, average='weighted'),
        'recall': recall_score(labels, predictions, average='weighted'),
    }

# 訓練結束後，手工檢查：
# 如果 F1=0.85 但 Recall=0.70 → 模型漏掉了 30% 的正面評論
# → 問題在於模型太保守（寧願誤判也不敢說「正面」）
# → 可能需要調整 decision threshold（不是 0.5 了）
```

**能讀懂這個，說明你真正理解了兩分類問題！**

---

### 1.4 模型保存：誰是真正的「大腦」？

**訓練完會產生這個結構**：
```
experiments/
├── checkpoint-100/          ← 第 100 步時保存的中間檢查點
│   ├── pytorch_model.bin    ← 訓練中的模型參數
│   ├── optimizer.pt         ← 優化器狀態（重新訓練時用）
│   └── ...
├── checkpoint-200/          ← 第 200 步時的檢查點
│   └── ...
└── final_model/             ← 最好的模型（根據驗證集 accuracy 選的）
    ├── config.json          ← 模型架構配置（不是參數）
    ├── pytorch_model.bin    ← 《真正的大腦》🧠 所有參數都在這！
    ├── tokenizer.json       ← 字典查表配置（不是模型）
    ├── special_tokens_map.json
    └── ...
```

**每個文件的作用**：

| 文件 | 大小 | 作用 | 推論時需要？ |
|------|------|------|------------|
| **pytorch_model.bin** | ~250MB | 存所有模型參數（權重、偏置） | ✅ **必須** |
| **config.json** | ~1KB | 定義模型架構（層數、隱藏維度等） | ✅ **必須** |
| **tokenizer.json** | ~200KB | 文本→token 的詞表和規則 | ✅ **必須** |
| optimizer.pt | ~250MB | 優化器狀態（Adam 的 m 和 v） | ❌ 推論不用 |
| training_args.bin | ~1KB | 訓練參數（batch_size 等） | ❌ 推論不用 |
| trainer_state.json | ~1KB | 訓練進度（已跑多少 step） | ❌ 推論不用 |

**為 LearnLink 後端部署：只需這三個**
```python
# 推論時的最小必要文件
model_dir = "./final_model"
tokenizer = AutoTokenizer.from_pretrained(model_dir)  # 用 config.json + tokenizer.json
model = AutoModelForSequenceClassification.from_pretrained(model_dir)  # 用 config.json + pytorch_model.bin

# 就能做推論！
```

**Pytorch_model.bin 到底裡面是什麼？**
```python
# 你可以打開看
import torch

# 加載 pytorch_model.bin
state_dict = torch.load("./final_model/pytorch_model.bin")

print(f"模型參數數量: {len(state_dict)} 個")
# 輸出類似：
# 模型參數數量: 201 個

# 看看有哪些
for name, param in list(state_dict.items())[:5]:
    print(f"{name}: shape={param.shape}, dtype={param.dtype}")
# 輸出類似：
# embeddings.word_embeddings.weight: shape=torch.Size([30522, 768]), dtype=torch.float32
# embeddings.position_embeddings.weight: shape=torch.Size([512, 768]), dtype=torch.float32
# embeddings.token_type_embeddings.weight: shape=torch.Size([2, 768]), dtype=torch.float32
# ...
```

✅ **你應該親自執行上面的代碼**，看看裡面真的是 200 多個矩陣乘法的權重！

---

## 🔧 Part 2：run_classification.py 代碼走讀

### 2.1 關鍵函數解析

**第 1 步：數據加載**
```python
def load_data(self, train_file, val_file, test_file) -> tuple:
    """為什麼要分開 train/val/test？"""
    
    # 讀取 CSV
    train_df = pd.read_csv(train_file)  # 8000 行
    val_df = pd.read_csv(val_file)      # 2000 行
    test_df = pd.read_csv(test_file)    # 5000 行
    
    # HuggingFace Dataset 格式
    train_dataset = Dataset.from_pandas(train_df)
    val_dataset = Dataset.from_pandas(val_df)
    test_dataset = Dataset.from_pandas(test_df)
    
    return train_dataset, val_dataset, test_dataset
```

**為什麼不能在測試集上訓練？**
```
訓練集：模型用來學習規律
驗證集：在訓練中檢查模型是否過擬合（early stopping）
測試集：最後評估「真實泛化能力」(不能在訓練中看!)

如果在測試集上訓練或調參：
  → "信息洩露"（data leakage）
  → 測試集評分虛高，真實部署會慘淡
  → 這是深度學習「大忌」！
```

---

**第 2 步：數據預處理 (preprocess_data)**
```python
def preprocess_data(self, dataset: Dataset) -> Dataset:
    
    # 加載分詞器（跟模型配對！）
    tokenizer = AutoTokenizer.from_pretrained(self.model_name)
    
    def tokenize_function(examples):
        """分詞函數 - 文本 → token ID"""
        
        # examples 是一個 dict，包含 'text' 和 'labels'
        # {'text': ['Review 1', 'Review 2', ...], 
        #  'labels': [0, 1, ...]}
        
        return tokenizer(
            examples['text'],                # 要分詞的文本
            padding='max_length',            # 短句子補 padding 到 128
            truncation=True,                 # 長句子截斷到 128
            max_length=self.max_seq_length,  # 128
            return_tensors='pt'              # 返回 PyTorch tensor
        )
        
        # 返回：
        # {
        #   'input_ids': [101, 2054, 2003, ...],  (128 個 token ID)
        #   'attention_mask': [1, 1, 1, ..., 0, 0]  (真實token=1, padding=0)
        # }
    
    # 把分詞函數應用到整個 dataset
    dataset = dataset.map(
        tokenize_function,
        batched=True,  # 批量處理，快速
        batch_size=1000,  # 一次處理 1000 個
        remove_columns=['text']  # 分詞後不再需要原文本
    )
    
    # 重命名標籤列（HF Trainer 期望的名字）
    dataset = dataset.rename_column('target', 'labels')
    
    # 設置格式讓 PyTorch 能用
    dataset.set_format('torch', columns=['input_ids', 'attention_mask', 'labels'])
    
    return dataset
```

**你應該理解的細節**：
```python
# Padding 和 Truncation 會發生什麼？

# 例子 1：短評論
text = "Great movie!"
tokens = [101, 2307, 3263, 999, 102]  # 5 個 token

# 用 max_length=128, padding='max_length'
result = {
    'input_ids': [101, 2307, 3263, 999, 102, 0, 0, 0, ..., 0],  # 補到 128
    'attention_mask': [1, 1, 1, 1, 1, 0, 0, 0, ..., 0]  # 前 5 個是真實，後 123 個是 padding
}

# 例子 2：超長評論
text = "This movie is absolutely... [200 tokens total]"

result = {
    'input_ids': [101, ..., 102],  # 只保留前 128 個 token，第 129+ 個被丟掉
    'attention_mask': [1, 1, ..., 1]  # 全是 1，沒有 padding
}

# 關鍵：Attention Mask
# - 1: 模型應該看這個 token
# - 0: 這是 padding，模型應該忽視
# → 不然 padding token（全是 0）會污染注意力權重
```

---

### 2.2 BERT 微調參數懶人包（核心 vs. 調校 vs. 參考）

時間寶貴，不是所有參數都同等重要。這份清單幫你分類哪些「必刻進大腦」，哪些「知道就好」：

#### 🧠 Level 1：核心靈魂（必須深度理解）

**為什麼？** 這三個參數直接決定了你的 3050 顯卡會不會爆炸。

| 參數 | 你的值 | 為什麼重要？ | 記住這個 |
|------|-------|-----------|---------|
| **max_seq_length** | 128 | BERT 計算量 ∝ 長度² → 125→512 顯存 ↑ 16 倍！ | 顯存殺手 |
| **learning_rate** | 2e-5 | 太大→模型學崩；太小→練到天荒地老 | 微調用極小值（2e-5～5e-5） |
| **per_device_train_batch_size** | 8 | 顯存 ↔ 訓練穩定度的平衡點。用 2 的次方（8/16/32） | 記住公式：Memory ∝ batch_size |

**實戰驗證**（你該做）：
```python
# max_seq_length 影響內存計算
Memory = batch_size × seq_length × hidden_size × 32 / 8
# 128: 8 × 128 × 768 × 32 / 8 = 400 MB ✓
# 256: 8 × 256 × 768 × 32 / 8 = 800 MB (會很擁擠)
# 512: 8 × 512 × 768 × 32 / 8 = 1.6 GB (即使沒 loss，也會 OOM!)

# learning_rate 的「大小」判斷
# 2e-5 = 0.00002 (微調用)
# 1e-4 = 0.0001  (太大，會震盪)
# 5e-6 = 0.000005 (太小，進度慢)
```

---

#### 🔧 Level 2：實戰必備（知道如何調校）

**何時用？** 當模型練不好、顯存還是不夠、或過擬合時。

| 參數                              | 現在設定 | 什麼時候要改？                                               |
| ------------------------------- | ---- | ----------------------------------------------------- |
| **num_train_epochs**            | 3    | 如果驗證準確率還在上升 → 加到 5；早就停滯了 → 減到 2                       |
| **gradient_accumulation_steps** | 2    | 顯卡塞不下更大 batch？公式：$Effective\ Batch = 8 \times 2 = 16$ |
| **warmup_steps**                | 100  | 訓練剛開始時，從 0 逐漸升到設定的學習率，防止「大腦抽筋」                        |

**梯度累積的救星公式**：
```
如果你想跑 batch_size=32 但 OOM，改法：
  per_device_train_batch_size = 8
  gradient_accumulation_steps = 4
  # 實際效果：每次更新用 8×4=32 樣本的梯度，但顯存只用 8 個樣本的空間！

不用梯度累積：1 epoch=1000 次更新
用梯度累積：1 epoch=250 次更新（但每次梯度來自 4 倍的數據）
→ 收斂慢點，但能用 8GB GPU 訓練
```

---

#### 📚 Level 3：要用再查（Reference Only）

**何時查？** 根本不用記，用到時再回來看。

| 參數 | 說明 | 預設就好？ |
|------|------|----------|
| **weight_decay** | 防止過擬合的數學手段，通常 0.01 就夠 | ✅ 用默認值 |
| **fp16** | 混合精度訓練：用 float16 部分層，float32 其他層。又快又省內存 | ✅ 3050 支持，開啟無害 |
| **logging_steps** | 每隔幾步印一次 Log，只是給「你看進度」用 | ✅ 100 就好 |

---

#### ⚡ Level 1 的深度理解（額外參考）

**為什麼 BERT 微調 learning_rate 要這麼小？**
```
- 從零訓練（從頭學）：LR 可用 1e-3～1e-4（大步子，快速探索）
- BERT 微調（只調整最後一層）：LR 用 2e-5～5e-5（小步子）
  
原因：BERT 已經在 102 億自然語言數據上訓練過了。
現在只是「輕輕推」它識別「情感」這一小件事。
大步子會摧毀它已學到的知識（Catastrophic Forgetting）。
```

**為什麼 batch_size=8？**
```
顯存容量：3050 有 8GB VRAM
單個樣本內存：128×768×32/8 = 50MB（僅僅 forward pass）
加上梯度、優化器狀態：×4～5 倍 = 200～250MB/樣本
8 個樣本：8 × 250MB = 2GB
加上其他開銷：總共 ≈ 4-5GB
留下 2-3GB 給操作系統 → 舒服訓練
```

---

**第 3 步：訓練參數完整代碼**
```python
training_args = TrainingArguments(
    # === 核心靈魂（Level 1）===
    per_device_train_batch_size=8,           # max_seq_length 和 batch_size 同樣重要！
    learning_rate=2e-5,                      # BERT 微調必用小值
    num_train_epochs=3,                      # 稍後可改（看過擬合情況）
    
    # === 實戰必備（Level 2）===
    gradient_accumulation_steps=2,           # 你的救星參數（顯存不夠時用）
    warmup_steps=100,                        # 防止訓練開始時的不穩定
    
    # === 參考即可（Level 3）===
    weight_decay=0.01,                       # 防過擬合（默認就好）
    fp16=self.device == "cuda",              # 加速（3050 支持）
    logging_steps=100,                       # 印 Log（進度用）
    
    # === 評估和保存邏輯 ===
    output_dir=self.output_dir,
    eval_strategy="steps",
    eval_steps=200,
    load_best_model_at_end=True,
    metric_for_best_model="accuracy",
    optim="adamw_8bit",
)
```

---

### 2.2 訓練流程視覺化

```
┌─────────────────────────────────────────────────────────────┐
│                     訓練流程（3 Epochs）                      │
└─────────────────────────────────────────────────────────────┘

EPOCH 1:
  [Step 0] Load batch 0-7 → Forward → Loss=0.95 
  [Step 100] Evaluate on val_dataset → Accuracy=0.75, F1=0.73
  [Step 200] Evaluate on val_dataset → Accuracy=0.80, F1=0.79 ← 最好迄今！存檔
  ...
  [Step 500] End of epoch 1

EPOCH 2:
  [Step 501] Loss=0.45
  [Step 600] Evaluate on val_dataset → Accuracy=0.85, F1=0.84 ← 更好了！更新最佳模型
  [Step 700] Evaluate on val_dataset → Accuracy=0.84, F1=0.83 ← 下降了，可能開始過擬合
  ...
  [Step 1000] End of epoch 2

EPOCH 3:
  [Step 1001] Loss=0.15
  [Step 1100] Evaluate on val_dataset → Accuracy=0.84, F1=0.83 ← 沒改進
  [Step 1200] Evaluate on val_dataset → Accuracy=0.83, F1=0.82 ← 下降
    → Early Stopping! 連續 3 次沒改進，停止訓練
  
  ✓ 加載最佳模型（Step 600 時的版本）
  ✓ 保存到 ./final_model/

└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Part 3：實驗結果回顧（已完成）

### 已完成：Max Sequence Length 對照

你已經完成三組 maxlen 對照，且每組都有完整評估輸出。

| 設定 | Accuracy | F1 | 訓練時間 | 結論 |
|---|---:|---:|---:|---|
| max_seq_length=64  | 0.8157 | 0.8157 | 3.41 分鐘 | 最快，但信息截斷明顯 |
| max_seq_length=128 | 0.8737 | 0.8736 | 4.03 分鐘 | 中間解，精度穩定 |
| max_seq_length=256 | 0.9030 | 0.9029 | 4.05 分鐘 | 最佳精度，時間幾乎不變 |

### 你的實際決策

- 目前最佳配置：`max_seq_length=256`
- 原因：相較 128，精度提升明顯；相較 64，避免過度截斷；訓練時間增加很小

### 後續（可選，不是必做）

- 可做：Learning Rate 小範圍測試（1e-5 / 2e-5 / 5e-5）
- 可先不做：Weight Decay 對照（你目前已明確選擇先不做）
- 可收尾：人工推論驗證 5-10 筆 + README 補齊結果表

---

## 💾 Part 4：訓練後的檔案理解

### 訓練完會看到什麼？

```
微調專項/
├── run_classification.py
├── run_maxlen_experiment.py
├── train.csv / val.csv / test.csv
├── results-maxlen-064/
│   ├── final_model/
│   └── results/
│       ├── eval_results.json
│       ├── evaluation_report.md
│       ├── confusion_matrix.png
│       └── evaluation_metrics.png
├── results-maxlen-128/
├── results-maxlen-256/
├── results-maxlen-summary.json
└── results-maxlen-analysis.txt
```

---

### 推論時的完整流程

```python
# 第 1 步：加載模型和分詞器
from transformers import AutoTokenizer, AutoModelForSequenceClassification
import torch

MODEL_PATH = "./results-maxlen-256/final_model"
tokenizer = AutoTokenizer.from_pretrained(MODEL_PATH)
model = AutoModelForSequenceClassification.from_pretrained(MODEL_PATH)

# 第 2 步：輸入文本
text = "This movie is absolutely amazing! Highly recommend!"

# 第 3 步：分詞
inputs = tokenizer(
    text,
    max_length=256,
    padding='max_length',
    truncation=True,
    return_tensors='pt'
)
# inputs = {
#   'input_ids': tensor([[101, 2054, 3123, ...]))
#   'attention_mask': tensor([[1, 1, 1, ...]])
# }

# 第 4 步：推論
model.eval()  # 切換到評估模式（dropout 不生效）
with torch.no_grad():  # 不記錄梯度（推論時不需要）
    outputs = model(**inputs)
    logits = outputs.logits  # shape: [1, 2]

# 第 5 步：轉換為概率
probs = torch.softmax(logits, dim=1)  # [1, 2] → 每個類別的概率
pred_label = torch.argmax(probs, dim=1).item()  # 0 或 1

# 第 6 步：輸出
label_map = {0: "Negative", 1: "Positive"}
print(f"預測: {label_map[pred_label]}")
print(f"信心: {probs[0][pred_label].item():.2%}")
```

**這就是 pytorch_model.bin 的全部用途！**

---

## 🎯 Part 5：核心內化清單

訓練結束後，用這份清單自測是否真的懂了：

- [x] **max_seq_length**：已完成 64/128/256 實測並能解釋取捨
- [ ] **Padding & Truncation**：知道 attention_mask 的用途（忽視 padding）
- [ ] **Compute Metrics**：會手寫 recall 和 precision 的計算，知道 weighted vs macro 的差異
- [ ] **Weight Decay**：目前列為可選（你已決定此階段先不做）
- [ ] **Learning Rate**：可作為下階段唯一追加實驗
- [x] **模型保存**：已完成多組模型與結果自動保存（maxlen 各目錄）
- [x] **Early Stopping**：已在實作中使用並完成訓練
- [x] **Gradient Accumulation**：已在實作參數中使用
- [ ] **推論流程**：可選收尾（補 5-10 筆人工驗證即可）

---

## 🚀 Next Steps（收尾版）

1. **固定交付版本**
    - 先以 `results-maxlen-256` 作為目前正式成果
    - `results-maxlen-summary.json` 作為實驗總覽

2. **可選收尾（1 小時內可完成）**
    - 手工推論驗證 5-10 筆
    - 在 README 補上 maxlen 對照結果表

3. **下一階段才做（非現在）**
    - Learning Rate 小範圍測試
    - BERT-base / RoBERTa 嘗試

你現在的狀態已經是可收工、可展示、可回顧複現的完整版本。
